# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Let's Encrypt
.well-known/acme-challenge/

# only XCF images allowed (they're 'source code' for PNGs)
images/*
!images/HeaderButtons/
!images/*.xcf
images/HeaderButtons/*
!images/HeaderButtons/*.xcf

# Fonts. They're open source, but there's no point in duplicating them in our repo when you can get them from official sources.
fonts/*